const fs = require('fs');
const path = require('path');
// const json = require(path.join(__dirname + '/lodash/config.json'));
const json = require('./lodash/config.json');
console.log(json);
console.log(path.join(__dirname + '/lodash/config.json'));